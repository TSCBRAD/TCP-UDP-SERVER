# Broadcast Server

A lightweight C++ server that receives JSON market data and sends it to different destinations using TCP or UDP.

## Features

- Receives JSON-formatted market data
- Sends data to multiple destinations based on protocol flags
- Supports TCP and UDP
- Implements TCP optimizations (e.g., TCP_CORK)
- Written in modern C++# TM-adaptive-tcp-udp-server
